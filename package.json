{
  "name": "quiemu-demo",
  "version": "1.0.0",
  "description": "",
  "main": "app/index.js",
  "scripts": {
    "build": "cd app && git clone https://github.com/Unzor/windows-2000-disk-image && cd windows-2000-disk-image && build.EXE && ping -n 3 127.0.0.1>nul && move win2k.disk ../win2k.disk && cd ../ && rmdir windows-2000-disk-image /s /q",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^16.0.4",
    "express": "^4.17.1",
    "pngjs": "^6.0.0",
    "rfb2": "^0.2.2",
    "express-all-allow": "^1.0.1",
    "socket.io": "^4.4.0",
    "socket.io-client": "^4.4.0"
  }
}
